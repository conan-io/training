package conan.ci.constant

class TrainingConstant {
    //TODO: Move all string constants to here and actually use them
    static CONAN_LOCKFILE_FILENAME = "conan.lock"
    static CONAN_BUILDORDER_FILENAME = "build-order.json"
    static CONAN_COMBINED_BUILDORDER_FILENAME = "combined-build-order.json"
    static CONAN_INFO_FILENAME = "conaninfo.txt"
    static DOCKER_IMAGE_FILENAME = "DockerImageName.txt"
}
